(function () {
    $('.slick-slider').slick({
        slidesToShow: 1, // Hiển thị 3 ảnh cùng lúc
        slidesToScroll: 1, // Trượt 1 ảnh mỗi lần
        infinite: true, // Trượt vô hạn
        autoplay: true, // Tự động trượt
        autoplaySpeed: 2600, // Tốc độ tự động trượt
        arrows: false
    });
})();

(function () {
    $('.slick-slider2').slick({
        slidesToShow: 1, // Hiển thị 3 ảnh cùng lúc
        slidesToScroll: 1, // Trượt 1 ảnh mỗi lần
        infinite: true, // Trượt vô hạn
        autoplay: true, // Tự động trượt
        autoplaySpeed: 3000, // Tốc độ tự động trượt
        arrows: false
    });
})();